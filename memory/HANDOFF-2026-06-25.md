# HANDOFF 2026-06-25 — research-hub

## このセッションの成果

保守3点 + Phase 2 着手:

1. **秘密設定を git 射程外へ退避**（前回 06-21 継続分の確定）
   - `.supabase-config` / `.discord-config` を `C:\dev\.secrets\`（git リポに属さない＝git clean 射程外）にコピー配置
   - research-hub の4スクリプト（generate-console-ready / learning-cli / seed-learning-topics / seed-claude-code-topics）の candidates を `.secrets` 最優先 → tak-work fallback に変更（非破壊・冗長化）
   - memory [[secret-config-file-disappeared]] に恒久対策実施済を追記。commit `3274d3c`（06-21）

2. **診断テスト記事 DIAGNOSTIC_* を一括削除**（task 0b）
   - navigator 記載は1件だったが、独立検証で**実3件**発覚（DIAGNOSTIC_TEST_REJECTED ×2 + DIAGNOSTIC_RPC_BYPASS_TEST、全て source_date=2999-12-31 でビューワー先頭固定）
   - ID 直打ち → **4条件シグネチャ一括削除**に切替（title LIKE DIAGNOSTIC% + summary=test + body=short + source_date=2999-12-31。冪等・誤削除ガード）
   - migration `supabase/migrations/20260621000001_delete_diagnostic_test_article.sql`。SQL Editor 実行で remaining=0、Worker GET で count=0 を独立検証。commit `196377b`

3. **学習マップ スタンプラリーUI を index.html に実装**（task 1）
   - ヘッダー「🎯 学習マップ」ボタン → 全画面ビュー切替（list/article と同じ show/hide パターン）
   - 8ジャンル（実データは7ジャンル127件、accounting は topic 0件で非表示）をジャンルカード（アコーディオン）+ 進捗バー + area別トピック一覧
   - トピックタップ → `related_article_ids[0]` → 既存 `showArticle()` で記事へ
   - データは初回オープン時に anon 遅延fetch、進捗はクライアント集計（RPC不要）
   - 検証: node --check OK／実データ dry-run で 7ジャンル sort_order順・記事リンク127/127解決・全体100%。commit `e75ac54`

## Tak の確定判断

- **学習マップのスコープ**: Claude Code 36件（原案）ではなく **learning_topics 全体（8ジャンル127件）** を採用。
  Claude Code 専属（claude_code_topics 36件）は別系統として残存。必要なら第2セクションとして後付け可能。
- 削除タスク 0b は archive ではなく **DELETE**（純粋なテスト残骸・隠れ行を残さない方針）。

## 次セッションの継続候補

1. **【実機確認・最優先】学習マップUIの目視確認**
   - GitHub Pages（https://takfukushima1978-spec.github.io/research-hub/）デプロイ反映後、「🎯 学習マップ」ボタン → カード開閉・進捗バー・トピックタップでの記事遷移を確認
   - CSS の崩れがあれば修正（ロジックは検証済、残りは見た目のみ）

2. **Phase 2 残タスク（navigator 📋 参照）** から次の一手:
   - **2. Discord 通知に学習マップ進捗バー埋め込み**（auto-research-morning-discord の embed に「🎯 X/Y (Z%)」。今回作った集計ロジックを Routine 側に展開）
   - **4. ビューワー URL hash 直リンク**（`index.html#article=<slug>`。Discord/メールから1タップで個別記事へ）
   - **5. DR 自動 completed 現象の調査**（[[dr-self-completion-mystery]]・supabase/functions/deep-research/index.ts 再読）

## 注意事項・前提

- articles 系の書き込み（削除/status変更）は専用 RPC が無く Worker GET も articles/tags のみ許可 → **SQL Editor or service_role が必須**（glossary_genre.sql と同じ）。
- 学習マップは anon REST 直叩き（ブラウザ）。`learning_topics` は anon に GRANT 済・`Accept-Profile: research` 必須。
- `05_gmail_to_supabase.py` は本マシンに**実体なし**（CLAUDE.md は git管理外と記載するが不在）。config の実行時消費者は research-hub の4スクリプトのみ。

## 自分層（読み物・0.5 併記）

- **今日やったこと**: 前から気になってた「秘密ファイルが消える問題」の恒久対策（git の届かない場所へ退避）と、ビューワー先頭に居座ってたテストのゴミ記事の掃除、そして学習マップを"スタンプラリー"として見える化した。
- **なぜ**: 前回の続き（.secrets 退避）が中途半端だったのと、基礎記事を全ジャンル完走したので「集めた実感」を出す画面が欲しかった。
- **モヤっと**: 掃除のとき navigator には「1件」と書いてあったのに実際は3件あった。記録した件数を鵜呑みにせず、毎回その場で全部数え直す方が安全だと再確認した。
